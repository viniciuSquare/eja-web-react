import styled from "styled-components";

export const ExitButtonStyled = styled.button`
  position: absolute;
  top: 5rem;
  right: 8rem;
  svg {
    color: gray;
    &:hover {
      color: black;
    }
  }
`