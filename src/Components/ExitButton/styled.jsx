import styled from "styled-components";

export const ExitButtonStyled = styled.button`
  position: absolute;
  bottom: 3rem;
  right: 5rem;
  svg {
    color: gray;
    &:hover {
      color: black;
    }
  }
`